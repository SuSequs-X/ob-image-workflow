# image-workflow

面向 Obsidian Markdown 写作场景的图片工作流插件。它的目标不是做“附件总管”，而是把笔记写作中最常见的图片问题压缩到一个闭环中：**粘贴后命名、尺寸控制、图片体检、批量排版、顺序重排、附件归档、安全清洗、日志回滚**。

> 核心机制：插件围绕 Markdown 图片嵌入链接工作，优先处理 `![[image.png]]`、`![[image.png|500]]`、`![](image.png)` 等图片引用，并通过预览、校验、日志和撤销降低断链风险。

---

## 适用场景

适合以下工作流：

- 写作时频繁粘贴截图、实验图、示意图；
- 希望图片文件名自动带有笔记语境；
- 希望统一图片显示宽度，例如全部设为 `500`；
- 希望检查当前笔记是否存在缺失图片、无尺寸图片、重复引用、同名风险；
- 希望按正文顺序重排图片编号，但不破坏原有语义名称；
- 希望把当前笔记引用的图片归档到同名 `.assets` 文件夹；
- 希望清洗图片路径，但不想因为同名文件导致误指向；
- 希望批量操作后可以撤销。

不建议将本插件扩展为视频、音频、PDF、Canvas、Excalidraw 等所有附件的统一管理器。当前版本的稳定边界是：**Markdown 笔记中的图片嵌入链接**。

---

## 新增痛点优化

### 1. 当前笔记图片体检

命令：

```text
检查当前笔记图片状态
```

该命令会扫描当前笔记中的图片嵌入，并输出以下指标：

| 指标 | 含义 |
|---|---|
| 图片嵌入 | 当前笔记检测到的图片引用总数 |
| 正常链接 | 可解析且无明显风险的图片链接 |
| 缺失文件 | 链接无法解析到真实图片文件 |
| 无尺寸图片 | 没有 `|500` 或类似尺寸后缀的图片 |
| 重复引用 | 同一张图片在当前笔记中被多次嵌入 |
| 同名风险 | 仓库中存在同名图片，短链接清洗可能误指向 |
| 外部链接 | `http`、`https`、`data:` 等外部图片 |
| 可安全清洗 | 可从路径链接安全收缩为文件名链接的图片 |

体检弹窗提供快捷动作：

- **统一无尺寸图片**；
- **预览安全清洗**；
- **重排图片编号**。

> 系统性风险：体检只基于当前 Obsidian 能解析到的链接状态。如果其他插件或同步盘正在异步修改文件，短时间内可能出现缓存未更新。高风险操作前建议重新执行一次体检。

---

### 2. 当前笔记图片尺寸统一

命令：

```text
统一当前笔记图片尺寸
```

支持四种范围：

| 范围 | 作用 |
|---|---|
| 仅无尺寸图片 | 只给没有尺寸的图片补充尺寸，推荐默认使用 |
| 全部图片 | 将当前笔记所有图片统一改为同一尺寸 |
| 仅当前标题下图片 | 只处理光标所在标题区块内的图片 |
| 清除全部尺寸 | 删除当前笔记图片链接中的尺寸后缀 |

示例：

```md
![[a.png]]
```

输入 `500` 后变为：

```md
![[a.png|500]]
```

已有尺寸会被覆盖：

```md
![[a.png|300]]
```

变为：

```md
![[a.png|500]]
```

该功能只修改 Markdown 正文，不重命名图片文件，风险低于文件移动和重命名。

---

### 3. 粘贴图片快速面板与尺寸预设

粘贴图片后，插件会显示图片设置面板。现在面板增加了尺寸预设按钮，例如：

```text
300 / 400 / 500 / 600 / 800
```

你可以在设置页中修改预设：

```text
尺寸快捷预设：300,400,500,600,800
```

目标是减少重复输入尺寸的操作成本。

> 前提条件：该功能依赖 Obsidian 的文件创建事件。图片粘贴后如果被其他插件立刻移动或重命名，本插件可能无法接管该文件。

---

### 4. 命名模板系统增强

设置项：

```text
图片命名模板
```

支持变量：

| 变量 | 含义 |
|---|---|
| `{{fileName}}` | 当前笔记文件名，不含扩展名 |
| `{{note}}` | 当前笔记文件名，等同于 `{{fileName}}` |
| `{{heading}}` | 当前光标所在标题 |
| `{{firstHeading}}` | 当前笔记第一个一级标题 |
| `{{folder}}` | 当前笔记所在文件夹名 |
| `{{dirName}}` | 当前笔记所在文件夹名，等同于 `{{folder}}` |
| `{{index}}` | 当前图片序号，按当前笔记图片数量推算 |

> `{{index}}` 默认不补零，例如 `1`、`2`、`3`。如果需要 `01`，可在设置页手动把“编号位数”改为 `2`。

### 序号冲突修复说明

当图片命名模板包含 `{{index}}` 时，插件现在会优先把末尾序号递增，而不是在完整文件名后再追加重复编号。

例如，当前已存在：

```md
![[张三-测试1-1.png]]
```

下一张图片会生成：

```md
![[张三-测试1-2.png]]
```

不会再生成：

```md
![[张三-测试1-1-1.png]]
```

这个修复适用于模板类似 `{{fileName}}-{{heading}}-{{index}}`、`{{note}}-{{heading}}-图{{index}}` 的场景。
| `{{DATE:YYYYMMDD}}` | 当前日期，格式由 Moment 解析 |
| `{{frontmatter:key}}` | 当前笔记 YAML 字段 |
| `{{imageNameKey}}` | 当前笔记 YAML 中的 `imageNameKey` 字段 |

推荐模板：

```text
{{fileName}}-{{heading}}-图{{index}}
```

示例：当前笔记为 `碳点-激发依赖性.md`，当前标题为 `表面态机制`，则图片名可能生成：

```text
碳点-激发依赖性-表面态机制-图1.png
```

该机制解决的是图片脱离笔记语境后难以识别的问题。

---

### 5. 语义保留式图片重排

命令：

```text
按当前文章顺序重排图片编号（预览）
```

设置项：

```text
重排命名模式
```

可选：

| 模式 | 结果 |
|---|---|
| 保留语义 | `笔记名-1-原短名.png` |
| 纯编号 | `笔记名-1.png` |

推荐使用 **保留语义**。它能让图片既保留正文顺序，又保留原文件名中的语义信息。

示例：

```text
表面态机制.png
缺陷态示意图.png
```

重排后：

```text
碳点激发依赖性-1-表面态机制.png
碳点激发依赖性-02-缺陷态示意图.png
```

> 系统性风险：重排会触发文件重命名，并依赖 Obsidian 自动更新 Markdown 链接。插件会写入操作日志，但仍建议先使用预览入口。

---

### 6. 当前笔记图片归档到同名附件文件夹

命令：

```text
归档当前笔记图片到同名附件文件夹（预览）
```

默认会把当前笔记引用的图片移动到：

```text
当前笔记名.assets/
```

示例：当前笔记为：

```text
碳点激发依赖性.md
```

归档目标为：

```text
碳点激发依赖性.assets/
  碳点激发依赖性-1-表面态机制.png
  碳点激发依赖性-02-缺陷态示意图.png
```

可在设置页修改归档文件夹模板：

```text
{{fileName}}.assets
```

> 系统性风险：归档同时涉及“移动文件 + 重命名文件 + 更新正文链接”。这是高风险写操作。请优先使用预览，并确认没有目标路径冲突。

---

### 7. 安全图片链接清洗

命令：

```text
预览图片文件名清洗
应用上次图片文件名清洗结果
打开图片清洗侧栏
```

该功能用于把路径型图片链接收缩为文件名链接。

例如：

```md
![[assets/images/a.png|500]]
```

安全条件满足时可清洗为：

```md
![[a.png|500]]
```

为了避免同名图片导致误指向，建议保持：

```text
要求文件名唯一：开启
```

插件会在预览中显示：

- 将修改哪些笔记；
- 每处链接的 Before / After；
- 被跳过的链接及原因；
- 是否存在同名风险、外部链接、无法解析路径。

---

## 主要命令

| 命令 | 用途 | 风险等级 |
|---|---|---|
| 当前行图片手动重命名 | 重命名当前行第一张图片 | 中 |
| 当前行图片设置尺寸 | 只修改当前行第一张图片尺寸 | 低 |
| 检查当前笔记图片状态 | 诊断当前笔记图片问题 | 低 |
| 统一当前笔记图片尺寸 | 批量设置或清除尺寸 | 低 |
| 按当前文章顺序重排图片编号（预览） | 预览重排文件名 | 低 |
| 按当前文章顺序重排图片编号（立即执行） | 直接执行重排 | 高 |
| 归档当前笔记图片到同名附件文件夹（预览） | 移动并重命名当前笔记图片 | 高 |
| 预览图片文件名清洗 | 扫描并生成清洗预览 | 低 |
| 应用上次图片文件名清洗结果 | 执行上次清洗预览 | 中 |
| 撤销上一次图片工作流操作 | 尝试回滚最近一次操作 | 中 |

---

## 操作日志与撤销

插件会为以下操作写入日志：

- 当前行图片设置尺寸；
- 当前行图片重命名；
- 当前笔记图片尺寸统一；
- 当前文章图片编号重排；
- 当前笔记图片归档；
- 图片链接清洗。

日志目录：

```text
.obsidian/plugins/image-workflow/logs/
```

其中：

```text
last-operation.json
```

用于撤销上一次操作。

撤销命令：

```text
撤销上一次图片工作流操作
```

撤销规则：

- 如果文件仍在新路径，且旧路径未被占用，则尝试改回旧路径；
- 如果笔记正文仍等于日志中的 `afterContent`，则恢复为 `beforeContent`；
- 如果笔记已经被用户继续修改，则跳过该笔记，避免覆盖新内容。

> 边界：撤销不是版本控制系统。它只能回滚最近一次插件记录的操作，且要求相关文件和正文状态没有被其他操作破坏。

---

## 推荐设置

### 保守写作模式

适合正式库、重要论文笔记、长期项目库。

```text
自动重命名：关闭
粘贴后询问尺寸：开启
要求文件名唯一：开启
最终确认：开启
重排命名模式：保留语义
尺寸预设：300,400,500,600,800
```

### 日常写作模式

适合普通知识库。

```text
自动重命名：关闭
粘贴后询问尺寸：开启
要求文件名唯一：开启
最终确认：开启
重排命名模式：保留语义
图片命名模板：{{fileName}}-{{heading}}-图{{index}}
```

### 高自动化模式

适合测试库或临时库，不建议直接用于正式库。

```text
自动重命名：开启
粘贴后询问尺寸：关闭
要求文件名唯一：关闭
最终确认：关闭
```

---

## 建议工作流

### 写作时

1. 粘贴图片；
2. 在弹窗中确认图片名；
3. 点击尺寸预设，例如 `500`；
4. 继续写作。

### 整理当前笔记时

1. 执行 `检查当前笔记图片状态`；
2. 如果存在无尺寸图片，执行 `统一当前笔记图片尺寸`；
3. 如果图片名混乱，执行 `按当前文章顺序重排图片编号（预览）`；
4. 如果图片散落，执行 `归档当前笔记图片到同名附件文件夹（预览）`；
5. 如需缩短路径，执行 `预览图片文件名清洗`。

### 批量清洗前

1. 保持 `要求文件名唯一` 开启；
2. 执行 `预览图片文件名清洗`；
3. 查看跳过原因和差异；
4. 确认后再执行 `应用上次图片文件名清洗结果`。

---

## 已知边界

1. **Obsidian 链接更新是异步的**  
   文件重命名后，Obsidian 需要一点时间更新正文链接。插件加入了短暂等待与校验，但大库或同步盘环境仍可能延迟。

2. **同名图片会提高误清洗风险**  
   如果仓库中存在多个 `image.png`，短链接 `![[image.png]]` 可能不稳定。建议保持唯一性检查。

3. **Markdown 图片语法尺寸不是 Obsidian 标准宽度语法**  
   对 `![](a.png)`，插件会把尺寸写入 alt 文本，例如 `![500](a.png)`。Obsidian 对 Wiki 嵌入 `![[a.png|500]]` 的支持更稳定。

4. **归档功能属于高风险写操作**  
   它会移动文件并更新链接。正式库中建议先备份，或至少先用预览确认。

5. **撤销不等于完整历史版本**  
   如果操作后又手动编辑了同一篇笔记，撤销会跳过，避免覆盖新编辑。

---

## 安装

将插件文件夹放入：

```text
.obsidian/plugins/image-workflow/
```

文件夹内应至少包含：

```text
main.js
manifest.json
styles.css
README.md
```

然后在 Obsidian 中进入：

```text
设置 → 第三方插件 → 已安装插件 → 启用“图片重命名与链接清洗”
```

---

## 版本说明

### v1.2.0

新增：

- 当前笔记图片体检；
- 当前笔记图片尺寸统一；
- 粘贴图片快速尺寸预设；
- 命名模板变量增强；
- 语义保留式图片重排；
- 当前笔记图片归档到同名附件文件夹；
- 更完整的 README。

保留：

- 当前行图片重命名；
- 当前行图片设置尺寸；
- 图片链接清洗侧栏；
- 操作日志；
- 上一次操作撤销。

### 8. 图片尺寸写入稳定性修复

本版针对“选择图片尺寸后没有写入”的问题做了专项修复。

修复点：

- 尺寸写入不再只依赖打开弹窗时记录的旧字符位置；
- 如果 Obsidian 在重命名后自动刷新了链接，插件会重新扫描当前行、附近行和当前笔记正文，重新定位目标图片链接；
- 文本替换优先使用 Obsidian 编辑器的 `replaceRange`，并在写入后校验正文是否真的包含新尺寸后缀；
- 写入失败时会显示明确提示，而不是静默失败。

推荐验证流程：

```text
粘贴图片 → 选择或输入尺寸 500 → 确认 → 检查正文是否变为 ![[图片名.png|500]]
```

如果图片已经存在于当前行，也可以使用：

```text
命令面板 → 当前行图片设置尺寸
```

该命令只修改尺寸，不重命名文件，适合排查尺寸写入问题。


## 2026-06-20 粘贴图片尺寸写入二次修复

### 问题现象

执行“粘贴图片 → 选择尺寸 500 → 确认”后，正文仍然只出现：

```md
![[图片名.png]]
```

而没有写入：

```md
![[图片名.png|500]]
```

### 根因

Obsidian 粘贴图片时，文件创建、正文链接插入、重命名后的链接刷新并不是严格同步完成。旧版本在确认弹窗后立即查找当前行链接；如果当时正文链接尚未插入，或者刚被 Obsidian 自动刷新，插件就找不到可替换的链接位置，导致尺寸写入被跳过。

### 本版修复

本版新增了强制尺寸写入链路：

1. 重命名后会在当前笔记全文中反复查找指向目标图片的嵌入链接；
2. 不再只依赖当前光标行，也不只依赖弹窗打开时记录的字符位置；
3. 支持在链接稍后才出现时继续等待并写入尺寸；
4. 支持通过文件名、完整路径、短路径三种方式匹配目标图片；
5. 写入后会再次校验正文中是否已经出现 `|500` 这类尺寸后缀。

### 推荐验证

安装后执行：

```text
粘贴图片 → 选择尺寸 500 → 确认
```

预期结果：

```md
![[图片名.png|500]]
```

如果仍未写入，优先检查“粘贴时弹出尺寸输入框”是否开启，以及是否有其他插件在粘贴后立即改写图片链接。

## 2026-06-20 直接重写尺寸链路修复

本版将“粘贴图片 → 重命名 → 选择尺寸 → 确认”的尺寸写入逻辑改为更直接的链路：

1. 粘贴图片后，先记录当前 Markdown 中刚插入图片链接所在行与字符区间；
2. 用户确认新文件名和尺寸后，先执行图片文件重命名；
3. 重命名完成后，不再等待 Obsidian 自动链接刷新结果作为主路径，而是直接把记录位置的图片嵌入重写为：

```md
![[图片名.png|500]]
```

如果原记录位置已经被 Obsidian 或其他插件刷新，插件会回退到当前行/全文查找对应图片链接，再执行同样的直接重写。

该修复针对的典型问题是：选择了 `500`，但正文仍然停留在：

```md
![[图片名.png]]
```



## 链接写入格式说明

图片写入使用 Obsidian 的图片嵌入格式 `![[图片.png]]`，带尺寸时为 `![[图片.png|500]]`。插件不会写成普通 Wiki 链接 `[[图片.png]]`。

## 修复：当前文章图片重排命名链式膨胀

本版修复了“当前文章图片重排预览”中出现的链式重复命名问题。

### 问题表现

旧逻辑在命名模板已经包含 `{{index}}` 时，仍然先生成一个带序号的基础名，再追加重排序号和原文件名，容易出现：

```md
![[张三-测试123-2-1-张三-测试1-1.png]]
```

这类结果属于错误命名。正确逻辑应由模板直接决定最终文件名。

### 新逻辑

如果图片命名模板包含 `{{index}}`，重排时直接按正文顺序把 `{{index}}` 替换为当前序号：

```text
{{fileName}}-{{heading}}-{{index}}
```

会生成：

```md
![[张三-测试1-1.png]]
![[张三-测试1-2.png]]
```

不会再追加旧文件名，也不会生成 `张三-测试1-1-1.png` 或 `张三-测试123-2-1-旧名.png`。

### 规则边界

- 模板包含 `{{index}}`：模板即完整命名规则，重排只替换序号。
- 模板不包含 `{{index}}`：插件才会使用“基础名 + 序号”的重排规则。
- “保留语义”只在模板不含 `{{index}}` 时追加旧文件名，避免链式命名膨胀。


## 修复：重排链路自查与代码清洗

本版对“当前文章图片重排”做了自查式修复，目标是避免再次出现 `张三-测试123-2-1-张三-测试1-1.png` 这类链式膨胀文件名。

### 修复点

1. **重排顺序改为读取当前笔记正文中的图片出现顺序**

   旧逻辑主要依赖 metadata cache 中的 embed 列表。新逻辑在构建重排计划时直接读取当前 Markdown 正文，解析 `![[...]]`、`[[...]]` 和 `![](...)`，再按正文出现顺序生成任务。

2. **模板包含 `{{index}}` 时禁止追加旧文件名**

   如果命名模板是：

   ```text
   {{fileName}}-{{heading}}-{{index}}
   ```

   重排只会替换 `{{index}}`，不会再叠加“重排序号”或“原文件名”。

3. **修复重复目标名检测**

   旧逻辑使用 `Set` 后再统计重复目标路径，重复项已经被去重，导致“目标文件名重复”检测失效。新逻辑改为数组计数，能正确阻止多个图片被重排或归档到同一路径。

4. **重排 / 归档后强制规范化图片嵌入链接**

   文件重命名完成后，插件会再次扫描当前笔记，把相关图片链接统一重写为图片嵌入格式：

   ```md
   ![[图片名.png]]
   ![[图片名.png|500]]
   ```

   这会修复 Obsidian 自动更新后残留的普通 Wiki 链接：

   ```md
   [[图片名.png]]
   ```

5. **修复序号递增正则**

   修复 `nextIndexedNameFromStem()` 中 `\d+` 转义错误，避免模板序号递增判断失效。

6. **修复空名判断正则**

   修复 `meaninglessRegex` 中空白字符 `\s` 转义错误，避免某些模板被错误判断为有效或无效。

### 当前推荐模板

```text
{{fileName}}-{{heading}}-{{index}}
```

编号位数建议保持：

```text
1
```

这样连续图片应生成：

```md
![[张三-测试1-1.png]]
![[张三-测试1-2.png]]
![[张三-测试1-3.png]]
```

如果选择尺寸 `500`，应生成：

```md
![[张三-测试1-1.png|500]]
![[张三-测试1-2.png|500]]
![[张三-测试1-3.png|500]]
```

---

## 代码重构与清洗记录（1.2.2）

本版主要处理内部结构，不改变插件的使用边界：仍然只围绕 Markdown 图片嵌入链接工作。

### 重构点

1. **统一图片链接解析入口**

   新增统一解析函数：

   ```js
   createImageLinkRegex()
   parseImageLinkMatch()
   makeImageLinkItemFromMatch()
   ```

   原先多个功能各自维护图片链接正则，容易出现“粘贴可识别、重排不可识别、体检又是另一套识别规则”的问题。现在当前行识别、全文扫描、体检、重排、归档等链路逐步收敛到同一套解析结构。

2. **清理重复字段语义**

   图片链接解析结果中明确保留：

   - `lineNumber`：行号，用于编辑器定位；
   - `lineText`：原始行文本，用于调试和后续扩展；
   - `line`：兼容旧 UI 显示逻辑，仍然保留为行号。

   这样避免旧代码里 `line` 同时表示“行文本”和“行号”的语义混乱。

3. **修复全局正则状态风险**

   所有逐行扫描现在按行创建新的 global regex，避免 JavaScript `RegExp.lastIndex` 在多行复用时造成漏匹配。

4. **删除无入口冗余方法**

   移除未被调用的 `getCurrentLineFirstEmbedFile()`，减少维护噪声。

5. **保留高风险链路的日志与回滚**

   本次没有削弱重命名、重排、归档的日志能力；涉及文件移动或正文改写的功能仍会写入 `.obsidian/plugins/image-workflow/logs/last-operation.json`。

### 自查结果

已执行：

```bash
node --check main.js
```

语法检查通过。

### 仍然保留的边界

插件没有改成多文件工程，也没有引入构建工具。原因是 Obsidian 社区插件在手动安装时通常依赖 `main.js + manifest.json + styles.css` 的简单结构；本版优先做**低迁移成本重构**，避免引入打包链导致新的安装风险。


## 1.2.3 设置页样式更新

本版增加了设置页 emoji 标识，并参考 `ob-plugin-list-toggler` 的设置组织方式，将设置页进一步调整为“顶部分区入口 + 宽松分区面板 + 透明原生设置项”的结构。

- 顶部标签保留 emoji，用于快速识别命名、重排、清洗、范围和操作。
- 分区标题增加 emoji，降低长设置页中的定位成本。
- 设置项本身不使用背景色，避免卡片化堆叠；只用边线、间距和标题层级区分内容。
- 按钮、下拉框、输入框使用更清晰的圆角和焦点状态。
- 保留之前的尺寸写入、强制 `![[...]]` 图片嵌入、链接模式、序号不补零、重排命名防叠加逻辑。

## 1.2.4 重排命名规则修复

### 修复点

- 当前文章图片重排时，图片名现在会严格按当前命名模板重新生成。
- `{{heading}}` 在重排时不再取当前光标所在标题，而是取每张图片在正文中所在位置的上级标题。
- 标题改名后，重新执行“当前文章图片重排”会按新标题生成新图片名。
- 当旧文件名已经明显是插件生成名时，重排不会再把旧图片名拼接到新文件名后面，避免出现：

```md
![[张三-1-张三-测试1-1.png]]
```

应生成：

```md
![[张三-新标题-1.png]]
![[张三-新标题-2.png]]
```

### 机制说明

重排命名现在遵循：

```text
读取当前 Markdown 正文
→ 按正文顺序解析图片链接
→ 根据每张图片所在行向上查找最近标题
→ 用当前命名模板渲染 {{fileName}} / {{heading}} / {{index}}
→ 生成目标图片名
```

因此，如果你修改了标题，只需要重新执行重排预览，预览中的新文件名就应跟随新标题变化。

---

## v1.1.0：融合图片展示模块

本版本以 `image-workflow` 为主插件，融合原 `image-grid` 的展示能力，不改变插件 ID 与现有数据目录。

### 新的推荐使用方式

需要自动图片网格的笔记，只在 Frontmatter 中加入：

```yaml
---
cssclasses:
  - image-grid
---
```

正文继续使用 Obsidian 原生图片语法：

```md
![[截图1.png]]
![[截图2.png]]
![[截图3.png]]

![[截图4.png]]
![[截图5.png]]
```

- 空行是硬分组边界；
- 2 张自动双列，3 张三列，4 张及以上四列换行；
- Live Preview 中悬停图片可直接调整 20%–300% 大小和常用比例；
- 比例菜单默认提供：原图、1:1、4:3、3:4、16:9、9:16；
- 阅读视图不显示操作按钮；
- 旧 `img-grid` / `img-grid-3` / `img-grid-4` 代码块仅作为兼容语法保留。

### 设置融合

插件设置新增「🖼️ 展示」标签，统一管理：

- Live Preview 悬停调节；
- 智能比例推荐；
- 默认/最大显示大小和快捷预设；
- 默认比例、适配方式、最大高度；
- 双列宽度预设及高级权重；
- 窄屏列数策略。

原 `image-workflow` 的 `|500` Wiki 尺寸功能继续保留，用于 Markdown 图片宽度；展示模块的 `scale=...` 用于网格内相对显示，两者职责不同。

### 融合审查结论

- 主插件 ID 继续为 `image-workflow`，现有配置可迁移；
- 展示模块设置只写入 `data.json` 的 `imageDisplay` 子树，不覆盖命名/清洗/重排配置；
- 不注册第二套设置页；
- 不增加“快速设置比例/大小”重复命令，日常调节统一由 Live Preview 悬停按钮完成；
- Block Decoration 使用 `StateField + EditorView.decorations.from(field)`，避免 CodeMirror 的 block decoration RangeError；
- 新增设置预设卡片全部使用弹性高度与响应式降列，避免固定高度造成文字遮挡；
- 旧代码块语法保留以兼容历史笔记。



## 1.1.1 — 原生 Wiki 图片宽度

图片展示模块的大小调节已与图片工作流统一：

- 不再生成新的 `{scale=150}`。
- Live Preview 悬停大小按钮直接写入 Obsidian 原生尺寸：`![[图片.png|500]]`。
- 比例仍可独立保存，例如：`![[图片.png|500]] {ratio=16:9}`。
- 大小预设复用插件原有“快捷尺寸预设”，默认值复用“统一尺寸默认值”。
- 旧 `{scale=...}` 仍可读取；下一次通过悬停按钮修改大小时，会自动移除旧 scale 并迁移为 `|宽度`。




## 1.1.3 — 图片额外说明

新增“当前行图片修改额外说明”。

图片文件名不会改变，只修改 Wiki 图片链接中的额外说明：

```md
![[123.png|500]]
```

填写“实验结果”后：

```md
![[123.png|实验结果|500]]
```

已有说明会被直接替换：

```md
![[123.png|旧说明|500]]
→
![[123.png|新说明|500]]
```

说明输入留空并确认时，会清除说明但保留尺寸：

```md
![[123.png|旧说明|500]]
→
![[123.png|500]]
```

入口：

- 命令面板：`当前行图片修改额外说明`
- Live Preview：图片悬停工具条 → `说明`
- 插件设置的快捷操作区

该功能不会重命名图片文件。


## 1.1.4 — 移除固定比例 + Live Preview 图片编号

本版进一步收敛图片展示能力：

- 移除固定比例功能，不再推荐或维护 `{ratio=16:9}` 这类写法。
- Live Preview 悬停工具条不再作为主入口提供比例裁切。
- 旧笔记中的 `{ratio=...}` 不会再影响展示；后续如果再调整图片宽度，会自动顺带清理旧 `ratio` / `aspect` 参数。
- 在 `cssclasses: image-grid` 的 Live Preview 自动网格中，每张图片左上角会显示编号徽标，方便区分同组图片。
  - 3 张并排时显示 `1 / 2 / 3`
  - 更多图片则按当前组顺序继续编号

保留的主路径：

- 图片大小：`![[图片.png|500]]`
- 图片额外说明：`![[图片.png|实验结果|500]]`
- 图片文件名：保持不变


## 1.1.5 — 简化网格设置

设置页已移除双列与网格的可调参数：

- 不再显示双列宽度预设
- 不再显示左右权重高级微调
- 不再显示窄屏保留双列开关

网格改为自动规则：

- 2 张：双列
- 3 张：三列
- 4 张及以上：四列换行
- 空行：结束当前图片组
- Live Preview：继续显示图片编号

原有网格渲染能力保留，只减少设置项。


## 1.1.6 — 展示逻辑收敛

本版进一步减少重复与相似功能：

- 阅读模式只负责展示，不再提供图片大小/说明编辑入口。
- Live Preview 才负责修改图片宽度与额外说明。
- 图片默认始终按原始比例显示，不再应用 ratio / fit / max-height 这类二次视觉变形。
- 图片大小继续使用 Obsidian 原生 Wiki 宽度，例如 `![[图片.png|500]]`。
- 设置页移除“图片适配”“最大图片高度”和只读的网格说明，避免重复。
- 所有图片与网格容器都限制为 `max-width: 100%`，避免大图把笔记正文撑宽或产生横向滚动。


## 1.1.7 — 恢复 Live Preview 修改功能

修复 1.1.6 中误伤 Live Preview 的回归：

- 阅读模式仍然只展示，不显示编辑控件。
- Live Preview 恢复图片悬停修改：
  - `−`
  - 当前宽度
  - `+`
  - `说明`
- img-grid Live Preview 图片编号继续保留。
- 原图比例、`max-width: 100%` 宽度保护继续保留。


## 1.1.8 — Live Preview 宽度/说明修复

- 彻底移除 Live Preview 工具条中的“原图/比例”控件。
- 宽度按钮现在会立即修改当前图片 DOM 的真实显示宽度，同时写回 `![[图片.png|500]]`。
- 不再出现仅数字改变、图片视觉大小不变的情况。
- 无原生宽度时不再错误显示 `20px`，优先使用插件统一默认宽度。
- “说明”按钮定位逻辑增强：
  - 先使用原行号
  - 行号失配时在附近搜索原始图片源码
  - 再退化为全篇唯一源码匹配/目标图片文件匹配
- 图片仍限制在笔记正文宽度内，不会横向撑破页面。


## 1.1.9 — img-grid-* 原生宽度修复

修复 legacy 代码块：

```md
```img-grid-3
![[a.png|说明|1000]]
![[b.png|说明|300]]
![[c.png|说明|800]]
```
```

此前 `|1000 / |300 / |800` 虽被解析，但网格槽位和 CSS 会把图片视觉宽度拉得接近一致。

现在：

- `|300` 会真实显示为 300px（如果当前列宽大于 300px）。
- `|800` / `|1000` 会按指定宽度尝试显示，但不会超过当前网格槽位/正文可用宽度。
- MarkdownRenderer 渲染完成后会再次显式应用原生宽度，防止主题或 Obsidian 内联样式覆盖。
- 未指定宽度时使用图片自然尺寸，并保持 `max-width: 100%` 防止撑破笔记。


## 1.1.10 — 允许图片横向超出正文

图片宽度现在严格优先服从 Wiki 尺寸：

```md
![[a.png|300]]
![[b.png|800]]
![[c.png|1000]]
```

其中 `300 / 800 / 1000` 会按真实像素宽度显示。

如果图片宽度超过正文或 img-grid 槽位，允许横向溢出，不再强制压缩到 `max-width: 100%`。

该改动同时作用于：

- `img-grid`
- `img-grid-3`
- `img-grid-4`
- `cssclasses: image-grid` 的 Live Preview 网格
- 阅读模式中的图片展示

图片高度仍保持自动比例，不拉伸变形。


## 1.1.11 — 图片说明与真实宽度统一

Wiki 图片语法正式统一为：

```md
![[图片文件.png|图片说明|宽度]]
```

例如：

```md
![[obsidian_image.png|Figure 1. 这是鸡蛋|100]]
```

插件解释为：

- 图片文件：`obsidian_image.png`
- 图注：`Figure 1. 这是鸡蛋`
- 显示宽度：`100px`

行为：

- 图注显示在图片下方。
- `100` 会真正应用到 `<img>` 的 CSS 宽度。
- 为防止 Obsidian/主题在异步渲染后覆盖尺寸，插件会在立即、下一动画帧和短延时后重新确认宽度。
- 同时支持 legacy `img-grid-*`、笔记级 `cssclasses: image-grid` Block Widget，以及光标进入图片组后的 Live Preview 原生行。
- 插件渲染时会把 Wiki 图片先转换为纯 `![[图片文件.png]]` 交给 Obsidian，再由插件自己渲染“说明”和“宽度”，避免多段 `|` 被 Obsidian 默认语义干扰。


## 1.1.12 — 图注居中 + 横向溢出范围收紧

- 图片说明始终显示在图片下方。
- 图片说明始终水平居中。
- 普通 `cssclasses: image-grid`、普通 Live Preview、阅读模式中的图片不再允许突破正文宽度。
- 只有 legacy 代码块允许真实横向溢出：
  - `img-grid`
  - `img-grid-3`
  - `img-grid-4`
- 在 legacy img-grid 中，`|1000` 可以真实显示为 1000px，即使超过正文宽度。
- 在普通 image-grid 中，`|1000` 仍会受当前正文宽度约束。


## 1.1.13 — 超宽 img-grid 列布局修复

修复 1.1.12 中超宽图片只溢出固定列、导致图片互相覆盖和图注错位的问题。

现在 legacy `img-grid / img-grid-3 / img-grid-4`：

- 网格列宽会跟随图片 `|宽度`。
- 同一列存在多行图片时，取该列最大声明宽度。
- 整个网格使用 `max-content`，允许整体横向超出正文。
- 图片不会再覆盖相邻图片。
- 图注始终紧贴对应图片下方，并以该图片宽度居中。
- 普通 `cssclasses: image-grid` 仍受正文宽度限制。


## 1.1.14 — 功能逻辑整理

本版以“减少重复、只保留当前实际工作流”为目标进行整理。

### 已移除的历史逻辑

- ratio / aspect 固定比例解析与写回
- RatioModal
- 智能比例推荐
- hover ratio 工具条
- 百分比大小快捷命令
- 隐藏的双列权重配置
- fit / max-height / aspect 展示变量

### 当前图片语法

```md
![[图片.png]]
![[图片.png|500]]
![[图片.png|图片说明|500]]
```

含义始终为：

1. 图片文件
2. 可选说明
3. 可选像素宽度

### 网格行为

- 普通 `cssclasses: image-grid`
  - 自动 2 / 3 / 4 列
  - 受正文宽度限制
  - Live Preview 显示编号
- `img-grid / img-grid-3 / img-grid-4`
  - 使用 `max-content` 列
  - 显式 `|宽度` 按真实像素执行
  - 整个网格允许横向突破正文
  - 各列不再互相覆盖
- 图注始终在对应图片下方居中

### 兼容

旧 `{scale=...}` 仍可读取；下一次修改图片宽度时会迁移为原生 `|宽度`。


## 1.1.15 — 边框宽度同步图片

图片宽度发生变化时，外层边框/容器也同步变化。

例如：

```md
![[a.png|说明|300]]
```

改成：

```md
![[a.png|说明|500]]
```

现在图片、外层边框和图注区域都会从 300px 同步变为 500px。

规则：

- 普通 `image-grid`：边框跟随图片宽度，但不超过正文宽度。
- `img-grid / img-grid-3 / img-grid-4`：边框跟随图片真实宽度，并可与图片一起横向突破正文。
- 图注继续位于边框内、图片下方居中。


## 1.1.16 — 设置页重构

设置分类由 7 个缩减为 5 个：

- 粘贴与命名
- 尺寸与展示
- 整理
- 清洗
- 未引用

调整重点：

- 将粘贴相关命名、附件处理、重名编号放在一起。
- 将宽度预设、默认宽度、粘贴宽度、Live Preview、图片说明放在一起。
- 将重排与归档放在“整理”。
- 将清洗规则、安全约束、扫描范围、清洗操作和侧边栏入口放在同一页。
- 未引用图片保持独立，因为它有独立的候选范围、引用范围和结果页。
- 删除单独的“范围/入口”和“操作”标签。
- 删除工作流模式、重复使用提示、历史比例/百分比说明等不必要设置文案。
- “粘贴默认尺寸”和“统一尺寸默认值”在设置页合并为“默认图片宽度”，修改时同步旧配置字段，保持兼容。


## 1.1.17 — Live Preview 超宽网格外框修复

修复 `img-grid / img-grid-3 / img-grid-4` 在 Live Preview 中图片宽度超过正文后，
代码块外框仍停留在正文宽度、导致右侧边框穿过图片的问题。

现在：

- legacy `img-grid-*` 的 Live Preview 外框跟随内部网格 `max-content` 宽度。
- 图片变宽时，外框同步扩展。
- 外框最小仍保持正文宽度，图片较小时不会缩成窄框。
- 只调整当前代码块宿主，不修改 `.cm-content`、`.cm-scroller` 或整个 CodeMirror 编辑器。
- 普通 `cssclasses: image-grid` 仍保持正文宽度约束。
- 图片、图注和边框继续使用同一 `|宽度`。


## 1.1.18 — Live Preview 真实外框同步

此前只扩大了插件内部的 image-grid 容器，部分 Obsidian 版本中真正绘制灰色边框的是更外层的 CodeMirror 代码块宿主，因此可见外框没有变化。

本版改为：

- 运行时定位实际的 `cm-embed-block` / `cm-preview-code-block`。
- 以内部 img-grid 的真实右边界计算外框宽度。
- 图片通过 Live Preview 修改宽度后，使用 `ResizeObserver` 自动同步外框。
- 外框最小保持原正文宽度，超宽图片出现时向右扩展。
- 不改变整个编辑器布局。


## 1.1.19 — 图片标题样式

图片第二段说明升级为“图片标题”，支持整体样式和局部文字样式。

示例：

```md
![[图片.png|这是**说明**|500]] {caption-color=#8b5cf6 caption-size=16}
```

支持：

- 整体标题颜色：`caption-color=#8b5cf6`
- 整体标题字号：`caption-size=16`
- 局部加粗：`**文字**`
- 局部斜体：`*文字*`
- 局部删除线：`~~文字~~`

Live Preview 的“说明”按钮改为“标题”。标题仍始终位于图片下方居中。


## 1.1.20 — 设置说明优化

图片标题示例统一为：

```md
![[图片.png|这是**说明**|500]] {caption-color=#8b5cf6 caption-size=16}
```

设置页重点优化“重排与归档 / 链接清洗 / 未引用图片”：

- **重排与归档**：明确哪些操作会真正重命名或移动图片文件，以及链接会同步更新。
- **链接清洗**：明确只修改笔记中的链接写法，不移动、不重命名图片文件；突出安全条件与“先预览后应用”的流程。
- **未引用图片**：区分候选图片范围与引用扫描范围，并明确扫描本身不会删除文件，删除前仍可在结果页确认。


## 1.2.0 — 清理重构 + 扩展优化

### 清理重构

- 删除已不再使用的 `workflowMode` 与 `applyWorkflowMode()`。
- 删除重复的 `batch-rename-all-images` 兼容命令入口。
- 保留旧清洗命令 ID 以兼容已有快捷键，但显示名称统一为“图片链接清洗”。
- 内嵌 image-grid 设置模块缩减为当前实际使用的 `gapRem / liveControls`，删除旧比例、fit、响应式、百分比缩放设置代码。
- 删除明确废弃的 ratio / hover ratio / 旧 image-grid settings CSS 规则。
- 保留 `{scale=...}` 的读取兼容，但新操作继续写入原生 Wiki `|宽度`。

### 扩展优化

- “重排与归档”新增 **图片体检与恢复**：
  - 打开当前笔记图片健康报告
  - 撤销上一次图片工作流操作
- “尺寸与展示”新增 **统一当前笔记图片宽度** 入口。
- Live Preview 自动网格图片增加“点击进入对应源码行”提示。
- 未引用图片新增 **近期保护**：
  - 默认保护最近 3 天创建的图片
  - 可设为 0 关闭
  - 扫描结果显示近期保护数量与引用扫描文档数量
  - 每个未引用结果明确显示“当前引用扫描范围内未发现引用”

### 保持不变

- 插件 ID：`image-workflow`
- 图片语法：
  `![[图片.png|这是**说明**|500]] {caption-color=#8b5cf6 caption-size=16}`
- 普通 `cssclasses: image-grid` 受正文宽度限制。
- legacy `img-grid / img-grid-3 / img-grid-4` 继续允许整体横向突破正文。


## 1.2.1 — 稳定性增强

本版重点不是扩展排版功能，而是统一危险操作、安全保护和诊断流程。

### 图片体检中心

- 每个图片问题可直接“定位源码”。
- 无尺寸图片可单项补默认宽度。
- 缺失文件与同名风险只报告和定位，不自动猜测修复。
- 顶部继续提供统一无尺寸、清洗预览、重排预览。

### 统一预览流程

- 重排：统一先预览，再执行。
- 旧“立即执行”命令 ID 保留兼容，但现在也会先打开预览。
- 归档：统一先预览目标路径，再执行。
- 链接清洗：保持预览 → 应用。
- 未引用删除：命令只打开结果页，不再直接对整批结果进入删除；删除前还会再次扫描引用。

### 未引用图片保护

新增：

- 最近 N 天保护。
- 当前活动笔记引用强制保护。
- 文件名 / 路径关键词保护。
- 原有忽略目录继续作为硬保护范围。

删除前会重新扫描；如果图片在扫描后新增了引用或命中保护规则，会自动跳过。

### Live Preview 图片定位

自动 `image-grid` 中图片编号/图片主体继续支持点击进入对应源码行；体检中心也使用同一“定位源码”工作流。


## 1.3.0 — 阅读模式图片查看器

阅读模式继续保持纯展示，但支持主动打开图片查看器：

- 双击图片
- Ctrl + 点击（macOS 为 Cmd + 点击）

查看器居中打开，并支持：

- 预览缩放：25%–300%，仅影响查看器。
- 正文图片宽度：复用插件已有宽度预设，可自定义输入并写回 Wiki 宽度。
- 旋转：左转 90°、右转 90°、恢复 0°；写回行尾 `{rotate=90}`。
- 图片标题：复用现有标题编辑器，继续支持颜色、字号、局部加粗 / 斜体 / 删除线。
- 定位源码：直接跳到当前图片对应的 Markdown 行。

示例：

```md
![[图片.png|这是**说明**|500]] {caption-color=#8b5cf6 caption-size=16 rotate=90}
```

阅读模式查看器只在用户主动触发后出现，不会在普通阅读视图中显示编辑按钮。


## 1.3.1 — 图片修改统一迁移到阅读模式

- Live Preview 不再显示图片宽度 / 标题悬停修改工具。
- 阅读模式图片查看器成为唯一的可视化图片修改入口。
- 设置页可控制：启用状态、双击、Ctrl/Cmd 点击、修饰键，以及缩放 / 宽度 / 旋转 / 标题 / 定位源码工具。
- 旧 `imageDisplay.interaction.liveControls` 会在迁移时强制关闭，避免历史 data.json 重新开启旧 UI。


## 1.3.2 — 精简设置与阅读查看器增强

设置页删除历史说明、重复帮助文字和大段操作解释，只保留必要的选项说明。

阅读模式双击图片打开的查看器新增：

- 在 Obsidian 文件列表定位图片文件
- 点击图片路径直接定位文件
- 复制图片 Vault 路径
- 复制 Wiki 图片链接
- 当前笔记上一张 / 下一张
- 定位 Markdown 源码

这些功能可在“尺寸与展示 → 查看器功能”中分别开关。


## 1.3.3 — UI 美化

- 设置页分区加入适量 emoji。
- 图片查看器、体检、清洗、未引用结果和确认弹窗统一视觉层级。
- 卡片、按钮、标题使用更统一的圆角、边框与留白。
